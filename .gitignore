# Dependencies
node_modules

# Build output
dist

# TypeScript build artifacts
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# Editor / OS / local tooling
.DS_Store
.claude
vite.config.js.timestamp-*.mjs
dummy/
.vercel

# Local graphify knowledge graph (regenerated via `graphify update`/`cluster-only` — see CLAUDE.md §18)
graphify-out/
